import React from 'react';
import TarotCardSelector from './TarotCardSelector';

/**
 * TarotCardSelector 사용 예제
 * 
 * 이 컴포넌트를 페이지에 추가하여 사용할 수 있습니다.
 */
const TarotCardSelectorExample: React.FC = () => {
  return (
    <div style={{ minHeight: '100vh', padding: '20px' }}>
      <TarotCardSelector />
    </div>
  );
};

export default TarotCardSelectorExample;

